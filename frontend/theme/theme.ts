import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#1976d2" },  // Mavi
    secondary: { main: "#ffffff" } // Beyaz
  },
});

export default theme;
